# Change this file name to .env

# Database Configuration
# Create a local PostgreSQL database and update with your credentials
DATABASE_URL="postgresql+psycopg_async://your_username:your_password@localhost:5432/your_db_name"

# JWT Authentication
# Generate a secure random secret key
# For Linux/macOS run this command: openssl rand -base64 32
# For Windows run: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY="your-secret-key-here"

# JWT Algorithm (keep as-is unless you have a specific reason to change)
ALGORITHM="HS256"

# App context (keep as is for local dev)
APP_ENV="local"